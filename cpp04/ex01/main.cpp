#include "Animal.hpp"
#include "Cat.hpp"
#include "Dog.hpp"
#include "WrongAnimal.hpp"
#include "WrongCat.hpp"
#include "Brain.hpp"

int main()
{
	// Dog originalBrain;
	// originalBrain.setIdea(0, "Original Idea");

	// Dog copyBrain = originalBrain;
	// copyBrain.setIdea(0, "Modified Copy Idea");
	// if (originalBrain.getIdea(0) == "Original Idea")
	// {
	// 	std::cout << "Deep copy test passed: Original is unchanged." << std::endl;
	// }
	// else
	// {
	// 	std::cout << "Shallow copy test failed: Original is modified." << std::endl;
	// }
	// originalBrain.setIdea(0, "Modified Original Idea");

	// if (copyBrain.getIdea(0) == "Modified Copy Idea")
	// {
	// 	std::cout << "Deep copy test passed: Copy is unchanged." << std::endl;
	// }
	// else
	// {
	// 	std::cout << "Shallow copy test failed: Copy is modified." << std::endl;
	// }

	// if(org.getIdea(0) == "org Idea")
	// {
	// 	std::cout << "Deep copy test passed: Original is unchanged." << std::endl;
	// }
	// else
	// {
	// 	std::cout << "Shallow copy test failed: Original is modified." << std::endl;
	// }

	return 0;
}
